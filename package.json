{
  "name": "robot-mirror",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build && touch dist/.nojekyll",
    "deploy": "gh-pages -d dist --dotfiles -m \"Deploy $(date -u +%FT%TZ)\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.34",
    "peerjs": "^1.5.5",
    "three": "^0.184.0"
  }
}
